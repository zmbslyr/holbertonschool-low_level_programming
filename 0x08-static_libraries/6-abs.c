#include "holberton.h"

/**
 * _abs - returns absolute value
 * @i: intiger
 * Return: return absolute integer for i
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
